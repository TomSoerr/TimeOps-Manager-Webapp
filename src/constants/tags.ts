/**
 * Constants related to tag management
 */
export const TAG_CONSTANTS = {
  /**
   * Default tag name used as a fallback when no specific tag is selected
   */
  DEFAULT_TAG_NAME: 'No Project',
};
